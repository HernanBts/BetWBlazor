@page "/countries/edit/{Id:int}"

<h3>@Localizer["Edit"] @Localizer["Country"]</h3>

@if (country is null)
{
    <Loading />
}
else
{
    <CountryForm @ref="form" Country="country" OnValidSubmit="EditAsync" ReturnAction="Return" />
}